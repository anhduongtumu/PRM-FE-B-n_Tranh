<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="?attr/colorSurfaceContainer"/>
    <!--
        A slightly off-white or light gray color is common.
        ?attr/colorSurfaceContainer is a good Material Design token.
        You can replace with a specific color like android:color="@android:color/white"
        or a custom color like android:color="@color/my_chat_input_bg"
    -->

    <corners android:radius="24dp"/>
    <!-- Adjust radius for more or less rounded corners -->

    <stroke
        android:width="1dp"
        android:color="?attr/colorOutline"/>
    <!--
        A subtle border. ?attr/colorOutline is a good Material Design token.
        You can replace with android:color="@color/my_chat_input_border"
        or a light gray like android:color="#CCCCCC"
    -->

    <padding
        android:left="12dp"
        android:top="10dp"
        android:right="12dp"
        android:bottom="10dp" />
    <!--
        This padding is internal to the background shape itself,
        affecting where the EditText's text cursor and content start.
        You might also set padding directly on the EditText view in your layout.
    -->
</shape>